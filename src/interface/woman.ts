// Interface de mujer

export interface Woman {
  title: string; // Nombre
  especialidad?: string; // Puede estar o no
  logro?: string; // Puede estar o no
  descripcion?: string; // "Qué hace"
  image?: string; // Por si después se agrega foto
}
